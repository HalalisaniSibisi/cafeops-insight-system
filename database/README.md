# Database
Contains SQL Server schema scripts, CREATE TABLE statements, and seed data.

Scripts should be run in the following order:
1. schema.sql — Create all tables
2. seed.sql — Insert initial data (if applicable)
